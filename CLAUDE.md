# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの現状

このリポジトリは現在**設計・構想フェーズのみ**で、アプリの実装コードはまだ存在しない（`package.json`・ソースファイル・ビルド設定なし）。中身は `doc/` 配下の設計ドキュメント（すべて日本語）と `README.md` のみ。実装を始める前提でこのリポジトリを触る場合は、まず `doc/` を読んで既存の構想を把握すること。

コミットメッセージ・ドキュメントはすべて日本語で書かれている。新規に追加する場合もこれに合わせる。

## プロジェクトの前提

- 本アプリは実運用のためではなく、**JavaScript学習を目的とした作品**として作られている（学習しながら手を止めずに作り進めることを優先）
- 題材は `README.md` の通り、**汎用的な「仕事で使う在庫管理」**。特定の実在組織への聞き取りには依存せず、架空データで進める
- プロダクトの中心思想は「在庫そのものを管理するのではなく、在庫状況から次に取るべき行動を提示する業務ナビゲーション」（詳細は `doc/apuriangenkei.md`）

## 開発者のスキル感・Claude Codeの振る舞い方針

開発者はプログラミング初学者〜初級。ただし「設計を考える→AIに実装させる→出てきたコードを読む→修正する→Gitで管理する」というAIネイティブな開発スタイルを実践中で、コードを丸投げで使うのではなく理解しながら進めることを重視している。これを踏まえて以下の方針で振る舞う。

### 実装は積極的に進めてよい
ファイル作成・編集・リファクタリング・テスト作成・ドキュメント作成・プロジェクト構造整理は、いちいち確認を取らず進めてよい。単純作業・定型作業に毎回説明は不要。

### 説明を添えるべき場面
以下は理由を添える。新しい概念の初出、新しいライブラリ／フレームワーク導入、ディレクトリ構成変更、データモデル変更、大規模なリファクタリング、アーキテクチャ変更、破壊的なGit操作。コマンドやコードを提示する際は「何をするか→なぜ今か→実行すると何が変わるか」を簡潔に。

### 初心者向けに過剰に単純化しない
実務で一般的な方法があるなら提示してよい。ただしその場合は「実務ではこの方法が一般的／今回は学習段階なのでここまで簡略化する」と差分を明示する。

### 過剰設計を避ける
個人開発・学習用の小規模アプリが中心。不要なフレームワーク・抽象化・クラス・依存関係・先回りの将来機能を追加しない。必要になったときに複雑化する。本リポジトリでは現段階でプレーンなHTML/CSS/JavaScriptを優先し、Node.js/npm/フレームワーク/ビルドツールは導入しない。

### 変更は小さな単位で
既存実装を確認してから変更する。大量のファイルを一度に書き換えるより、既存実装→変更理由→変更箇所→変更後の挙動が追える小さな単位で進める。

### JavaScriptの理解度の目安
- 理解済み：`const`/`let`、関数、条件分岐、配列、オブジェクト、DOM操作、イベント、ブラウザでJSが動く仕組み
- 未習得：Promise、async/await、fetch、モジュール、npm/package.json、Node.js、TypeScript、React等のフレームワーク、ビルドツール、テスト
- これらを使うコードを書く場合は、使う理由と処理内容を一言添える

### Git・ターミナルの理解度の目安
- 使える：`status`/`add`/`commit`/`push`/`pull`/`branch`/`switch`/`switch -c`/`diff`。main上で直接作業せずブランチを切る運用に移行済み
- 未習得：rebase、cherry-pick、reset、revert、stash、本格的なmerge conflict解消、detached HEAD、tag/release、CI/CD。使う場合は必ず何をしているか説明する
- 破壊的操作（`git reset --hard`、force push、ブランチ削除、大規模rebase、history書き換え）は原則避ける。行う場合は目的と影響を先に説明してから実行する
- ターミナルは `cd`/`ls`/`pwd`/`mkdir` 程度。PATH・環境変数・権限・pipe/redirect・grep/findなどは説明を添えると助かるレベル

## ドキュメント構成

- `README.md`：プロジェクトの一行説明
- `doc/apuriangenkei.md`：構想を統合した読み物。プロダクトの中心思想（「注意を管理するUI」「Excel＝正本、Web＝行動管理」というExcel連携の方向性、判定ルールの仮説）、JS学習プロジェクトとしての位置付けが最もまとまっている。現時点の到達段階も記載あり（構想→実装メモ→リサーチ→設計書→実装、のうち実装メモ完了・リサーチ着手段階）
- `doc/sekkei.gensoku.md`：初期の実装メモ。荒い箇条書きで未完成（末尾に空項目が残っている）。`apuriangenkei.md` と同じ構想を実装寄りに書いたもの
- `doc/situmonhyou.md`：**【保留】整骨院向けの聞き取り質問票**。一時期「実家の整骨院向けアプリ」として検討していた名残で、冒頭に注記の通り現在は着手対象外。JS制作にもっと慣れてから着手する将来案として、聞き取り計画をそのまま保持している。**現在の学習プロジェクトの参照先としては使わない**

## 未確定事項（`apuriangenkei.md` / `sekkei.gensoku.md` より）

- ローカルExcelファイルとの接続・同期をV0.1に入れるかどうかは未確定（仮データ→CSV→.xlsx読み込み→双方向連携、という段階的発展を想定）
- 判定ルール（発注点、緊急発注の閾値、入荷予定日超過の扱いなど）はまだ正式仕様ではなく、これからリサーチして確定させる段階
- 発注点・最低在庫・安全在庫の使い分けなど、Notionにリサーチタスクとして登録済みの4件（`apuriangenkei.md` 末尾参照）が確定前提

## 開発コマンド

実装コードが存在しないため、ビルド・lint・テストのコマンドは現時点でなし。
